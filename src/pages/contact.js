import * as React from 'react';
import Layout from '../components/Layout/Layout';

const Contact = (props) => {
  return (
    <Layout>
      <div>test</div>
    </Layout>
  );
};

export default Contact;
