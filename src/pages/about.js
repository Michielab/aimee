import * as React from 'react';
import Layout from '../components/Layout/Layout';

const About = (props) => {
  return (
    <Layout>
      <div>test</div>
    </Layout>
  );
};

export default About;
